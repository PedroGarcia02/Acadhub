<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Painel Administrativo</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <%- include('../includes/navbar') %>

  <div class="max-w-7xl mx-auto p-4">
    <h1 class="text-3xl font-bold text-center mb-6">Painel Administrativo</h1>

    <% if (error) { %>
          <p class="text-red-500 text-center"><%= error %></p>
    <% } %>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
 
      <a href="/cadastro" class="bg-white shadow-lg rounded-lg p-6 flex flex-col items-center justify-center hover:bg-blue-100 transition">
        <div class="text-blue-600 text-4xl mb-2">👤</div>
        <div class="text-lg font-semibold text-center">Cadastrar Usuários</div>
      </a>

      <a href="/cadastroCursos" class="bg-white shadow-lg rounded-lg p-6 flex flex-col items-center justify-center hover:bg-blue-100 transition">
        <div class="text-green-600 text-4xl mb-2">📚</div>
        <div class="text-lg font-semibold text-center">Cadastrar Cursos</div>
      </a>

      <% if (session.user && session.user.tipo === 'admin') { %>
      <a href="/cadastroInstituicao" class="bg-white shadow-lg rounded-lg p-6 flex flex-col items-center justify-center hover:bg-blue-100 transition">
        <div class="text-yellow-600 text-4xl mb-2">🏫</div>
        <div class="text-lg font-semibold text-center">Cadastrar Instituições</div>
      </a>

      <a href="/trocarInstituicao" class="bg-white shadow-lg rounded-lg p-6 flex flex-col items-center justify-center hover:bg-blue-100 transition">
        <div class="text-red-600 text-4xl mb-2">🔄</div>
        <div class="text-lg font-semibold text-center">Trocar Instituição</div>
      </a>
      <% } %>

    </div>
  </div>
</body>
</html>

